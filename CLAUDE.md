# 制片帽 · 项目记忆（CLAUDE.md）

> 这是「制片帽」项目的长期记忆，给之后每次对话打底。用中文、说人话。
> 维护原则：**只写核实过的事实，不确定的标 `TODO: 需帽帽确认`，绝不编造。**
> 最近更新：2026-06-08（v1.0，标签按 时代/类型/题材/调性 归类；攻克豆瓣 /j/tag 抓取、设为主力源；三源加 1 小时缓存）

---

## 一、项目背景

- **项目名**：制片帽（Producer Hat）
- **定位**：面向影视制片人的 AI 工具，核心目的是**提高制片工作效率**。
- **目标用户**：影视行业从业者 / 制片人（先给帽帽自己用，未来面向广大从业者）。
- **核心功能规划（三大板块）**：
  | 板块 | 做什么 | 状态 |
  |---|---|---|
  | **策划 · 选题雷达** | 在大买家之前，从各大文学网站发现还没卖、有改编潜力的好故事，给出改编分析报告 | **当前主攻** |
  | **制作 · 剧本拆解** | 把剧本拆成顺场表、人物戏量、场景/道具清单等 | 规划中 |
  | **发行** | 数据监测、档期竞品、宣发节点等 | 待定 |
- **平台节奏**：先做电脑端（网页），成熟后上手机端（APK）。

---

## 二、技术栈（按当前代码实际情况，非臆测）

| 层 | 用了什么 | 大白话 |
|---|---|---|
| 前端 | **原生 HTML / CSS / JavaScript**，无框架、无构建、无第三方 JS 库 | 就是最朴素的网页三件套。没用 React/Vue 这类"脚手架"。一个单页应用，靠 JS 切换"屏幕"。 |
| 前端状态/存储 | **浏览器 localStorage** | 账号、口味偏好、收藏、点赞都存在用户自己浏览器里，没上传。 |
| 后端 | **Python 3.14.3 标准库**（`http.server` + `urllib`），**零第三方依赖** | 一个脚本就是后端，不用 pip 装任何东西，谁的 Mac 都能跑。 |
| 数据库 | **没有数据库**。数据放在 JSON / JS 文件里 | 候选作品库 = `backend/corpus.json`；线上静态演示数据 = `app/data/recommendations.js`。 |
| 大模型 | **DeepSeek API**：模型 `deepseek-v4-pro`（思考型、最强；旧名 `deepseek-chat` 将于 2026/07/24 弃用），接口 `https://api.deepseek.com/chat/completions`（OpenAI 兼容格式） | 大脑用国产 DeepSeek。key 放本地 `backend/key.txt`，不进代码/git。单次约 80 秒（思考型偏慢），按周用没问题。 |
| 字体 | 造字工房 **新简黑 / 骏雅 / 书研**（.otf，本地打包，共约 5.7MB） | 文艺调性。⚠️ 见【已知问题】授权事项。 |
| 部署 | **GitHub Pages**（静态前端，push 自动部署）+ **本地 Python 后端**（AI 功能） | 线上：https://xmoses909-debug.github.io/zhipianmao/ ；仓库：github.com/xmoses909-debug/zhipianmao |

> **重要**：AI 实时选片只在**本地后端**可用；**线上（Pages）是纯静态版**，AI 按钮会优雅提示"需本地后端"。原因：静态网页不能安全地直接调大模型（密钥会暴露）。

---

## 三、目录结构

```
帽制片/                      ← 项目根（注意：文件夹名是"帽制片"，产品名是"制片帽"）
├── app/                     前端网页（线上部署的就是这个文件夹）
│   ├── index.html           页面骨架：欢迎 / 注册 / 选偏好 / 主页 四屏 + 详情弹窗
│   ├── app.js               全部交互逻辑：屏幕路由、偏好选择、AI 选片调用、卡片/弹窗渲染
│   ├── styles.css           样式：编辑部风格、CSS 变量、响应式
│   ├── data/recommendations.js  静态演示数据（线上无后端时显示的那批）
│   ├── manifest.json        PWA 配置（"添加到主屏幕"、出 APK 用）
│   ├── sw.js                Service Worker —— 当前暂停（见架构决策）
│   ├── icon-192/512.png     App 图标（赤陶底 + 鸭舌帽 logo）
│   └── fonts/               造字工房字体（xinjianhei/junya/shuyan.otf）
├── backend/                 本地 AI 后端（不部署到线上）
│   ├── server.py            后端：服务 app/ + /api/discover（组候选池→调 DeepSeek 选片）
│   ├── scraper.py           实时抓取：晋江排行榜→新书候选（扩库的"手"，纯标准库）
│   ├── corpus.json          兜底候选库（10 部已核实；抓取失败时降级用它）
│   ├── key.txt              你的 DeepSeek key（git 忽略，不上传）
│   ├── .env                 key 的另一种放法（git 忽略）
│   └── README.md            后端怎么跑
├── agent/                   "智能体"的概念雏形（给后端做参考，非运行代码）
│   ├── skills/选品与改编分析.md   选品分析的流程 + 评分规则（相当于给大模型的"工作手册"）
│   └── memory/taste-profile.json  口味画像记忆雏形
├── 启动.command             双击启动（纯静态，无 AI）
├── 启动-AI.command          双击启动（带 DeepSeek 后端，AI 模式）
├── 分享给朋友.command       双击：后端 + Cloudflare 隧道 → 公网网址，发朋友试用（免备案）
├── serve.py                 本地无缓存静态服务器（静态模式/预览用）
├── 需求文档-v1.md            早期需求与路线图
├── README.md                项目总览 + 怎么运行
├── .github/workflows/pages.yml  GitHub 自动部署配置
└── .claude/launch.json      Claude 预览用配置
```

---

## 四、已做的架构决策（每条带「为什么」+「放弃了什么」）

| 决策 | 为什么这么选 | 放弃 / 不选 |
|---|---|---|
| **前端纯原生、无框架、无构建** | 简单、零依赖、非技术用户能双击就开、易托管；项目目前不复杂，框架是负担 | 放弃 React/Vue（徒增复杂度与构建步骤） |
| **后端用 Python 标准库、零依赖** | 用户 Mac 自带 python3，不用装任何东西就能跑 | 放弃 Node/Flask/FastAPI（都要装依赖、抬高门槛） |
| **不用数据库，数据放 JSON/JS 文件** | 测试阶段够用、零运维 | 放弃数据库（真多用户上线时才需要，见 TODO） |
| **大模型走国产 DeepSeek，且设计成可切换** | 面向国内、国内可访问、便宜、合规；`deepseek-v4-pro`（思考型）读小说+文学判断强；想省钱可一行切 `deepseek-v4-flash` | 放弃 Claude API（不正式对华、访问与付费麻烦）；暂不买 MiniMax 包年（测试期按量更划算） |
| **选品评分：好故事 > 题材**（综合分 = 故事力×0.6 + 题材×0.4） | 帽帽要的是大模型的**文学判断力**，不止标签匹配；宁可推题材不全对但故事极好的 | 放弃"纯标签匹配" |
| **选品策略：选品前置**（找未售的早期作品，不追热榜） | 热门 IP 早被头部公司买空，推了也没用 | 放弃"追热度榜" |
| **候选库锚定真实事实 + 大模型只做判断** | 书名/作者/链接/字数/梗概来自真实库，**不让大模型编造书目**（对制片人是硬伤）；判断/分析才交给大模型 | 放弃"让大模型自由生成书单"（会幻觉假书） |
| **三源自动抓，豆瓣为主力**（帽帽明确要求 2026-06-08） | 帽帽要豆瓣的小众原创连载。豆瓣 robots 禁 `/j/`、有封查风险——**帽帽拍板接受**；缓解：真浏览器 UA + 1 小时缓存 + 不暴力并发。发现入口 `/j/tag/<标签>`（从豆瓣 JS 源码里挖出来的，无需 cookie、可翻页） | 放弃"只精选收录豆瓣"（那样豆瓣进不了池）；**商用前仍需谈授权**（robots 能爬 ≠ 商用授权） |
| **三源按配额组池**（豆瓣14+晋江6+番茄4，豆瓣为主） | 晋江一次 400 本会把别源挤光；配额保证三源都进大模型视野，且豆瓣占主力 | 放弃"纯按分数混排"（会变单一源） |
| **标签按 4 类归组**（时代/类型/题材/调性） | 原来一堆扁平标签太乱；归类后用户好选、前后端词表对齐 | 放弃扁平标签；老用户旧词自动迁移（日系文艺→文艺等） |
| **耽美/ABO 不作过审门槛** | 帽帽口径：过审是剧本层改人物关系的事，不该把好故事滤掉 | 放弃"按过审先筛掉耽美" |
| **账号 = 本地演示版（localStorage）** | 静态托管做不到真账号 | 真多用户账号需后端 + 数据库（见 TODO） |
| **部署 GitHub Pages** | 免费、push 自动部署、能很快上线让人用 | ⚠️ 这是**临时**选择：github.io 国内访问不稳，面向大众时要换国内托管 |
| **小范围试用走 Cloudflare 免费隧道**（`分享给朋友.command`） | 给几个朋友试不值得为此备案/租服务器；隧道免费、免备案、即开即用，且 **key 不出本机**（最安全） | 放弃"为试用就上国内云"（备案数天~数周+成本）；代价=本机要开着、网址每次变、国内偶尔慢 |
| **后端能认 `PORT` 环境变量** | 让 Claude 预览工具/隧道能分配端口，不写死 4173 | —— |
| **Service Worker 暂停** | 反复编辑时旧缓存老干扰；先求"改了就能看到" | 部署稳定后再启用稳健版 SW |
| **key 走本地 `key.txt`/`.env`（git 忽略）** | 密钥绝不进代码、git、聊天 | 放弃硬编码、放弃前端直连大模型 |
| **字体造字工房**（标题骏雅、正文骏雅、点缀书研） | 文艺调性；帽帽有授权。标题原用新简黑但太粗"坨在一起"，改骏雅 | 放弃新简黑做标题；⚠️ 商用授权见 TODO |
| **openclaw 不作产品后端** | 它是单用户、聊天驱动的私人助理，不适合做多用户网页后端；用专用轻量后端反而模型可切换、好维护 | 放弃把 openclaw 当产品后端（可作帽帽个人助理或参考） |

---

## 五、当前进度

### ✅ 已完成
- **策划-选题雷达 前端**：欢迎 → 注册 → 选偏好（题材/来源/体量/状态 + 自定义需求对话框）→ 主页四屏流程；AI 小助手（动态日期+幽默提示）；偏好可自选；双分卡片（故事力/题材）；完整改编报告弹窗；收藏/点赞拍掉。
- **UI 美化**：造字工房字体、编辑部风格、鸭舌帽 logo + App 图标。
- **静态版已上线** GitHub Pages（线上可访问，AI 按钮在线上优雅降级）。
- **本地 AI 后端**：`/api/discover` 调 DeepSeek 做真实选片分析；**plumbing 已验证通过**（服务 App、接口、缺 key 报错、语法全过）。
- **🧠 AI 闭环已用真 key 跑通（2026-06-07）**：`deepseek-v4-pro` 真实测试成功——83 秒返回，按自定义需求从候选库挑 3 部、打分（故事×0.6+题材×0.4，数值核对无误）、写改编分析；**无编造书目、链接真实、主动点出耽美过审/制景成本等难点**。这是"大脑真转起来"的首次确认。
- **🕷️ 三源实时抓取（2026-06-08）**：`scraper.py` 自动抓 **晋江排行榜**(~400/次，避开热榜已售 IP、过滤影视化痕迹) + **番茄**(首页/榜单→详情页 `__INITIAL_STATE__`，~30/次；状态码 0=完结/1=连载)；**豆瓣**因 robots 禁 `/j/` 书单接口、不自动抓，以本地精选纳入。三源【按配额】组池（晋江13+番茄5+豆瓣6）保证都露脸；>200 万字爽文过滤掉。端到端实测挑出跨源 5 部（含番茄《娇养玫瑰》）。
- **🐛 修两个 bug（2026-06-08）**：① 两态标签后清掉历史"不感冒"脏数据（曾导致过度过滤、选片空结果）；② 没有完全贴合的需求也**必给 3-5 部最接近的**（绝不空手），并说明差距。
- **🔗 "分享给朋友"已打通（2026-06-07）**：`分享给朋友.command` 用 Cloudflare 免费隧道把本机后端捅到公网，公网访问实测 HTTP 200。免备案、免租服务器，供小范围试用（代价：本机要开着）。
- **🎛️ UI 改版**：选片移到"进入制片帽"那一刻触发→全屏进度浮层→出结果；题材标签改两态（选中✓/未选）；**全面去除 AI/DeepSeek/模型字样，统一"制片帽"品牌**。
- **🏷️ 标签归类（2026-06-08）**：题材标签从一堆扁平 → 按 **时代/类型/题材/调性** 4 类归组；前后端 genres 词表对齐；老用户旧标签自动迁移。浏览器实测渲染正确。
- **🟢 豆瓣攻克为主力源（2026-06-08）**：从豆瓣 JS 源码挖出 `/j/tag/<标签>` 接口（无需 cookie、可翻页），按题材标签抓豆瓣**原创连载** ~310 本；三源池豆瓣占主力（14/24）。端到端实测：选出含豆瓣《十夜》《线引画境》，详情页链接正确、可点开。**选品前置的"小众早期连载"这下真有了。**
- **数据可行性验证**：豆瓣阅读、晋江公开页可抓；晋江书页有反爬混淆（用搜索索引）；番茄网页 JS 动态、抓不到。
- **真实候选内容**：corpus.json 10 部已核实作品。

### 🚧 进行中
- **扩充候选库**：晋江排行榜已接（每次抓 ~400 本）。**待办**：豆瓣阅读列表藏 JS 接口（详情页好抓、列表难找）、番茄 JS 动态抓不到、多源合并去重、抓"更新/更冷门"的榜以更贴"选品前置"。
- **小范围试用 vs 正式上线**：试用走免费 Cloudflare 隧道（已通）；正式面向大众仍需**国内云 + ICP 备案**（待帽帽定，见隐私 vs 国内可用性那条）。

### 📋 下一步 TODO
1. ~~帽帽填 key → 跑真实测试~~ ✅ **已完成（2026-06-07）**：`deepseek-v4-pro` 真实选片跑通。
2. **扩充候选库：让后端实时抓豆瓣/晋江新书**（最难、最关键的硬骨头）。
3. 每周自动爬新书（定时代理）。
4. 多用户**真账号 + 真数据库**（正式上线时）。
5. **换国内托管**（面向大众；github.io 国内不稳）。
6. 出 APK（PWABuilder，对着线上网址生成）。
7. 字体**子集化**减体积（尤其书研 2.7MB）。
8. 制作板块（剧本拆解）、发行板块。
9. 重新启用 Service Worker（部署稳健版）。

---

## 六、编码约定

- **文件命名**：前端文件小写英文（`index.html`/`app.js`/`styles.css`）；中文文档/脚本用中文名（`需求文档-v1.md`/`启动.command`）。
- **JavaScript**：ES5 风格（`var` + `function`，兼容性好、无需构建）；整个 app.js 用一个 IIFE 包住，避免污染全局；DOM 操作用原生（无 jQuery/框架）。
- **Python**：只用标准库；`server.py` 单文件；中文注释说明"为什么"。
- **数据 schema**：作品对象用统一结构（`bookCard` 渲染）；**真实事实**（书名/链接/字数/梗概）与**主观判断**（故事力/分析）分开。
- **样式**：颜色/字体走 CSS 变量（`--serif`/`--sans`/`--art`/`--accent`/`--story` 青=故事力 /`--match` 金=题材）。
- **注释风格**：中文，重点解释"为什么"而非"是什么"。

---

## 七、协作偏好（每次对话都遵守）

- 帽帽是**编程零基础的 vibe coder**——解释技术决策用通俗类比，别堆术语。
- 重要技术选择给 **trade-off 表格 + 你的推荐**，别只罗列选项。
- 所有**模型名、API、库版本、参数**必须核实过再写，**不许 hallucinate**；拿不准就标 `TODO: 需帽帽确认`。
- **直接表达**，不要"当然可以""没问题"这类填充话。
- 隐私敏感，**倾向避免依赖中国服务器的服务** —— ⚠️ **这条和当前"用 DeepSeek + 计划国内托管"直接冲突，见【已知问题】第 1 条，待帽帽定夺。**

---

## 八、已知问题 & 待解决

1. **`TODO: 需帽帽确认`｜隐私偏好 vs 国产方案的冲突**：协作偏好里写"避免依赖中国服务器"，但实际决策是 DeepSeek（中国大模型）+ 计划国内托管——因为面向国内、GitHub 国内不稳、Claude 对华不便。**两者只能选一个优先：数据隐私 还是 国内可用性？请帽帽明确。** 在确认前，按"国内可用性优先"执行（因为产品面向国内用户）。
2. **`TODO: 需帽帽确认`｜字体商用授权**：三款造字工房字体文件名都带 **Noncommercial**（非商用版）。产品将来面向广大从业者（商用），正式商用上线前需确认商用授权。帽帽说已处理，此处仅留档提醒。
3. ~~**AI 闭环尚未用真 key 验证**~~ ✅ **已验证（2026-06-07）**：真 key + `deepseek-v4-pro` 真实选片跑通（83 秒/次、按需求挑 3 部、打分、写分析、无编造）。**遗留体验问题**：思考型模型单次 ~80 秒偏慢，交互靠"思考中"提示缓冲；若嫌慢可切 `deepseek-v4-flash` 换速度。
4. ~~候选库只有 10 部~~ ✅ **三源实时抓取（豆瓣为主力）**：豆瓣(~310，`/j/tag/<标签>` 按题材抓原创连载) + 晋江(~400) + 番茄(~30)，带 1 小时缓存。**风险/遗留**：① 豆瓣 robots **禁 `/j/`、有封查风险**——**帽帽已拍板接受**，已做真 UA + 缓存 + 适度请求量缓解（若被封需再想办法）；② 番茄列表偏男频玄幻爽文（已用 >200 万字上限 + 配额缓解）；③ **`TODO: 需帽帽确认`｜商用版权**：抓三家内容做商业产品涉版权，能爬 ≠ 商用授权，长远要谈授权/合作（和字体授权同类问题）。
5. **账号是本地演示版**，不是真账号系统（真 auth 需后端+数据库）。
6. **github.io 国内访问可能不稳**（线上托管的临时性问题）。
7. **`backend/__pycache__/*.pyc` 一度被纳入 git**（Python 字节码缓存，本不该提交）——已加入忽略并清理。
8. **`TODO: 需帽帽确认`｜终端报错**：帽帽的 `~/.zshrc` 有一行 openclaw 残留（`source ~/.openclaw/completions/openclaw.zsh`），每次开终端报"找不到文件"。要不要我帮你删掉那行？（openclaw 本机其实没装。）
